trainer:
  class: Trainer
  params:
    num_epochs: 20

dataset:
  class: MNISTDataloader
  params:
    root: data
    batch_size: 256
    num_workers: 0

model:
  class: LeNet

optimizer:
  class: Adam
  params:
    lr: 1.e-3

scheduler:
  class: StepLR
  params:
    step_size: 10
