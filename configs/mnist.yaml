trainer:
  name: ClassificationTrainer
  num_epochs: 20

optimizer:
  name: Adam
  lr: 1.e-3

scheduler:
  name: StepLR
  step_size: 10

model:
  name: LeNet

dataset:
  name: MNISTDataloader
  root: data
  batch_size: 32
  num_workers: 8
