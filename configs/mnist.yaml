trainer:
  name: Trainer
  num_epochs: 20

dataset:
  name: MNISTDataloader
  root: data
  batch_size: 256
  num_workers: 0

model:
  name: LeNet

optimizer:
  name: Adam
  lr: 1.e-3

scheduler:
  name: StepLR
  step_size: 10
